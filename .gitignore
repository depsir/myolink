# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Claude Code — impostazioni locali
.claude/settings.local.json

# Web app
node_modules/
app/dist/

# Arduino / build
build/
*.bin
*.elf
*.hex
*.map

# Log e catture del segnale
*.log
*.csv
!firmware/**/*.csv

# Le registrazioni del 4 settembre: 90 MB, di cui 89 di mp4 (uno solo da 53 MB).
# Stanno sul disco e non si cancellano — sono le uniche etichette che esisteranno
# mai, vedi docs/PIANO-fase6.md — ma non entrano nella storia di git: un binario
# da 53 MB non si toglie più senza riscriverla, e il clone se lo porta dietro per
# sempre. Come tenerle davvero (Git LFS? solo i CSV?) è una decisione rimandata.
docs/samples/
